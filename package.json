{
  "name": "ideal-postcodes",
  "version": "1.0.0",
  "dependencies": {
    "swagger-repo": "~2.0.0-rc.11"
  },
  "private": true,
  "scripts": {
    "start": "swagger-repo serve",
    "build": "swagger-repo build -o public",
    "test": "swagger-repo validate"
  }
}
