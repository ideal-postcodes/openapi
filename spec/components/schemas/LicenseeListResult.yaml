title: LicenseeListResult
type: object
properties:
  licensees:
    type: array
    items:
      $ref: '#/components/schemas/Licensee'
    description: ''
  hasMore:
    type: boolean
    description: >-
      Returns true if there are more licensees listed after the maximum number
      of results as implied by `limit`
