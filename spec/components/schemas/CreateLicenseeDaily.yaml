title: CreateLicenseeDaily
type: object
properties:
  limit:
    type: number
    description: >-
      The maximum number of lookups this Licensee can perform in a day. Set to
      `null` to deactivate the limit
