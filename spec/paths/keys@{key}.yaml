get:
  tags:
    - Keys
  summary: Check Key Usability
  description: |
    Currently returns whether the key is useable via the `available` property.

    You may use this method to discover if the key is useable before making further requests.

    When the `user_token` is not provided, this API returns public information on key. 

    ### Testing

    To test your implementation of our API, you may use the following test keys.

    - **iddqd** Availability will return as `true`
    - **idkfa** Availability will return as `false`

  operationId: CheckKeyUsability
  parameters:
    - name: key
      in: path
      description: Your API Key. Typically begins `ak_`
      required: true
      style: simple
      schema:
        type: string
    - name: user_token
      in: query
      description: |
        If `user_token` is omitted, the API will only indicate whether the API Key is usable for a given session

        If `user_token` is provided, the API will return a detailed API Key response object containing more information
      style: form
      schema:
        type: string
  responses:
    '200':
      description: Success
      headers: {}
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/KeyUsabilityResponseSchema'
    '400':
      description: Bad request
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/ErrorResponseSchema'
    '402':
      description: Request failed
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/ErrorResponseSchema'
    '404':
      description: Resource not found
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/ErrorResponseSchema'
    '500':
      description: Server error
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/ErrorResponseSchema'
    '503':
      description: Rate limit exceeded
  deprecated: false
  security: []
